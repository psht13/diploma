# Polish Log

## 2026-03-15

- Прочитано обов'язковий контекст:
  - `docs/codex/STATE.md`;
  - `docs/codex/POLISH_LOG.md`;
  - `docs/codex/MANUAL_ACTIONS.md`;
  - `assets/Зміст курсового проєкту.docx`;
  - `assets/маг робота як шаблон оформлення.docx`;
  - `output/coursework_draft_ua_final_polished.docx`;
  - `output/coursework_draft_ua_final_polished_manual.docx`;
  - увесь код у `prototype/`;
  - `prototype/README.md`;
  - `package.json`.

- Проведено page-by-page аудит двох фінальних `.docx` через PDF-рендер і текстовий витяг сторінок.
- Зафіксовано, що `output/coursework_draft_ua_final_polished_manual.docx` не можна брати за еталон:
  - чужа тема на титулі;
  - зламаний `ABSTRACT`;
  - зміщені перші сторінки;
  - додатковий випадковий уламок тексту.

- Для реальної браузерної перевірки:
  - підтверджено доступність локальної `Ollama`;
  - підтверджено наявність моделі `llama3.1:8b`;
  - запущено прототип на `http://localhost:4173/prototype/`;
  - пройдено e2e-сценарій через Playwright.

- Під час Playwright-прогону знайдено й локально виправлено три дрібні, але реальні проблеми:
  - короткий тайм-аут Ollama не давав стабільно пройти генерацію вправи з `llama3.1:8b`;
  - `SessionMemory.resetForExercise()` не скидав `firstFailure`;
  - у UI не показувалася очікувана назва функції, що ускладнювало відтворюваний браузерний сценарій.

- Після правок повторно підтверджено:
  - `npm test` -> 27/27;
  - `npm run check` -> успішно;
  - повний Playwright/Ollama сценарій -> пройдено.

- Для документа створено новий артефакт `output/coursework_draft_ua_submission_ready.docx`.
- У submission-ready документі виконано:
  - нормалізацію шрифтів і мовних тегів (`uk-UA`, `en-US`);
  - переформатування 3.5.1 з окремими формульними рядками;
  - переробку 3.5.4 у більш академічні абзаци з lead-in;
  - оновлення 4.1, 4.4.1, 4.4.2, 4.5 під фактичний стан локальної Ollama й браузерної валідації;
  - зменшення кількості ручних заглушок шляхом вилучення таблиці 4.7;
  - повторне увімкнення заголовків таблиць при перенесенні;
  - оновлення змісту під фактичні сторінки.

- Фінальний рендер `coursework_draft_ua_submission_ready.docx` перевірено в PDF:
  - 39 сторінок;
  - 4 рисунки;
  - 13 таблиць;
  - 4 додатки;
  - 30 джерел.

