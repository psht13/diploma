# Поточний стан роботи

## Статус

- Фінальний polish pass завершено 15.03.2026.
- Оновлений прототип і документ синхронізовані між собою.
- Основний фінальний артефакт документа: `output/coursework_draft_ua_final_polished.docx`.

## Що зафіксовано в коді

- У `prototype/` прибрано залежність explain-flow лише від regex:
  - `TutorPolicy` підтримує явні `interactionMode: "explain"` і `forceExplanation`;
  - натискання кнопки `Пояснити тему` тепер гарантовано веде до `concept_explanation`.
- Додано полегшений JSON-експорт поточної сесії:
  - `topic`;
  - `difficulty`;
  - `attemptsCount`;
  - `firstFailure`;
  - `lastAction`;
  - `transcript`;
  - `finalRunStatus`;
  - `timestamp`.
- Додано легкий sanity-check script: `npm run check`.
- README оновлено відповідно до нових можливостей.

## Що зафіксовано в тестах

- `npm test` проходить повністю: 26/26 тестів.
- Додано unit-тест на явний explain-mode без reliance лише на текст запиту.
- Додано unit-тест на JSON-експорт сесії.
- `npm run check` проходить.

## Що зафіксовано в документі

- Створено `output/coursework_draft_ua_final_polished.docx` на основі `output/coursework_draft_ua_fixed.docx`, без переписування з нуля.
- Фінальний PDF-рендер перевірено:
  - 43 сторінки;
  - без порожніх сторінок;
  - зміст оновлено під фактичні сторінки;
  - діаграми 3.3 і 4.1 збільшено;
  - для таблиць задано повторення заголовка на наступних сторінках;
  - розділи 4.2 і 4.4.1 оновлено під explain-mode, session export і `npm run check`;
  - джерело `Ollama; Meta` розділено на два окремі записи.
- Поточні облікові параметри документа:
  - 43 с.;
  - 4 рис.;
  - 14 табл.;
  - 4 дод.;
  - 30 джерел.

## Відомі обмеження

- У поточному середовищі локальний `ollama` не встановлено, тому повний browser-based прогін із реальною локальною моделлю не виконувався.
- Документ чесно не містить вигаданих педагогічних результатів, p-values, effect sizes або розміру вибірки.

## Ручно перед здачею

- Заповнити УДК.
- Заповнити ПІБ студента і номер групи.
- Заповнити ПІБ, ступінь і посаду керівника.
- Проставити службові підписи, дати й реквізити.
- Додати реальні результати педагогічного експерименту лише якщо такий експеримент реально проведено.
