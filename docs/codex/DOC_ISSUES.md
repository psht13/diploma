# Стан документа після виправлень

## Автоматично виправлено в `output/coursework_draft_ua_fixed.docx`

1. Зміст
- Згенеровано реальний зміст із номерами сторінок.
- У документі більше немає тексту `Оновіть поле змісту у Word...`.

2. Нумерація сторінок
- Додано page numbering у колонтитулі за зразком шаблону.
- У PDF-рендері сторінки відображаються починаючи з другої сторінки документа.

3. Сирі технічні артефакти
- Видалено `{cite('S11', 'S12')}`.
- Видалено згадки `docs/codex/...` з тіла документа.
- Видалено текстові плейсхолдери для рисунків.

4. Діаграми
- Вставлено 4 реальні figure assets:
  - компонентну діаграму АІНС;
  - діаграму `UserState` / `SessionMemory`;
  - sequence diagram для сценарію генерації вправи, тестування і фідбеку;
  - runtime-схему прототипу.

5. Академічні підписи
- Додано нумеровані підписи для всіх таблиць і рисунків.
- Після підсилення експериментального блоку загальна кількість таблиць становить 14.

6. Анотація та статистика документа
- Автоматично підставлено кількість сторінок, рисунків, таблиць, додатків і джерел.
- Поточний фінальний рендер: 41 сторінка, 4 рисунки, 14 таблиць, 4 додатки, 29 джерел.

7. Верстка
- Повністю порожні сторінки прибрано.
- Фінальний PDF-рендер не містить порожніх сторінок.

8. Стиль і узгодження з кодом
- Розділ 4 синхронізовано з фактичним станом `prototype/` після виправлень.
- Експериментальний блок посилено:
  - функціональна валідація;
  - гіпотези;
  - змінні;
  - метрики;
  - план статистичного аналізу;
  - таблиця-шаблон для ручного внесення результатів.

## Що лишилося ручним

- ПІБ студента.
- Номер групи.
- ПІБ, посада і ступінь керівника.
- УДК, якщо не буде визначено достовірно.
- Підписи та службові реквізити.
- Реальні результати педагогічного експерименту.
