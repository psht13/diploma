# Final Doc Fixes

- Згенеровано новий фінальний файл `output/coursework_draft_ua_submission_ready_v3.docx` без перезапису попередніх версій.
- Відновлено секційні параметри з stable base `assets/coursework_draft_ua_submission_ready.docx`:
  - `w:sectPr`;
  - `pgSz`;
  - `pgMar`;
  - `titlePg`;
  - `header/footer` references;
  - page numbering field `PAGE`.
- Після відновлення секцій повернулася нормальна ширина текстового блоку, зник глобальний зсув вправо і знову з’явилися номери сторінок.
- Перегенеровано 4 diagram assets і оновлено їх як у основному тексті, так і в додатку А:
  - `figure_3_1_component_architecture.png`;
  - `figure_3_2_session_memory.png`;
  - `figure_3_3_sequence.png`;
  - `figure_4_1_runtime.png`.
- Додаток А переверстано на окремі сторінки для кожної діаграми, щоб зменшені копії лишалися читабельними.
- Для таблиць додано row-level `cantSplit` і повтор заголовного рядка; окремо компактніше оформлено таблицю 3.2, щоб прибрати невдалий розрив.
- Перевірено абзацні відступи:
  - основний текст лишився з нормальним абзацним відступом першого рядка;
  - у комірках таблиць `left indent = 0`, `first line indent = 0`;
  - прихованого глобального зсуву абзаців після repair не лишилося.
- Легко дошліфовано мову в місцях змішаного англ.+укр. наративу без чіпання усталених скорочень типу `LLM`, `JSON`, `API`, `RAG`, `Web Worker`, `Ollama`, `Llama`.
- Зміст повторно звірено з фінальним рендером:
  - `ВСТУП - 7`;
  - `РОЗДІЛ 3 - 15`;
  - `РОЗДІЛ 4 - 24`;
  - `Додаток А - 37`;
  - `Додаток Б - 40`;
  - `Додатки В/Г - 41`.
