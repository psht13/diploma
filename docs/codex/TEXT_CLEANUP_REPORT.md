# Text Cleanup Report

## Що прибрано як сліди машинної або технічної редактури

- Прибрано формулювання на кшталт:
  - `чесно оформлений дизайн експерименту`;
  - `не вигадуються` / `не вигадувалися`;
  - `реальні артефакти середовища`;
  - `реальний кодовий артефакт`;
  - `автентичні фрагменти промптів`;
  - `ШІ-репетитор systems`;
  - `ШІ-репетиторing`.
- З основного тексту й таблиць вилучено прямі dev-oriented згадки:
  - `npm test`;
  - `npm run check`;
  - `Playwright`.
- Із наративного тексту прибрано згадки про ручний ввід / ручне втручання / `manual`.

## Що було переформульовано

- Анотацію та abstract зроблено нейтральнішими й академічнішими.
- У вступі вирівняно стиль переліку завдань.
- У розділі 3 зменшено кількість сирих code-oriented назв у концептуальному викладі; таблиці й підписи до діаграм переведено на природнішу українську лексику.
- У розділі 4.4.1 та таблиці 4.3 типи перевірки подано як:
  - `модульні тести`;
  - `контрольний сценарій перевірки`;
  - `браузерна функціональна перевірка`.
- У додатку Б вступний текст до прикладів промптів очищено від мета-лексики про їхню `автентичність`.
- У діаграмах прибрано англомовні службові підписи на кшталт `Chat UI`, `SessionMemory`, `TutorPolicy`, `JSON validation + retry`, `Friendly error handling`.

## Формат сторінки

- Так, формат сторінки виправлено на A4.
- Фактичний розмір сторінки у `v4`: `210 x 297 мм`.
- Поля, header/footer і пагінацію збережено.

## Бібліографія

- Так, запис `[21]` виправлено.
- Гібридну назву `ШІ-репетиторing...` замінено на оригінальну англомовну назву джерела:
  - `AI tutoring can safely and effectively support students: An exploratory RCT in UK classrooms`.

## Що ще лишилося прибрати вручну

- Додаткових слідів машинної/технічної редактури, які неможливо було виправити автоматично, не виявлено.
- Слово `systems` лишається лише в оригінальних англомовних назвах джерел у бібліографії; це не є помилкою і не потребує ручного очищення.
